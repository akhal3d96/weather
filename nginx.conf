server { 
 listen 80;
 server_name expert-i.espace.ws app.expert-i.com;
 location / {
   root /usr/share/nginx/html;
   try_files $uri /index.html;
 }
 location /health_check {
   access_log off;
   default_type text/plain;
   return 200 "success\n";
 }
}
